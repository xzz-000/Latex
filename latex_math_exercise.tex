\documentclass[UTF8]{ctexart}
\begin{document}

函数 \( y = \sin x \) 在整个实数域 \( \mathbb{R} \) 上可导，即对于任意 \( x \in \mathbb{R} \)，其导数 \( y' = \cos x \) 存在。

\end{document}
%\( ... \) 是 LaTeX 原生语法，而 $ ... $ 是 TeX 的旧语法。
%（\\）更加有优势：
%更清晰的代码可读性（明确区分数学模式和文本）。
%避免与货币符号 $ 冲突（例如 $100 表示 100 美元，而非数学公式）。
%某些宏包或编辑器对 \( ... \) 的支持更稳定。